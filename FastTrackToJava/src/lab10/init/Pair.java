package lab10.init;

public class Pair {
    // Define a generic type that holds two objects of any type.
    // Make it immutable and create two methods that return the two objects.

    // Implement the hash code, equals and toString methods
}
